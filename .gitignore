# Hugo build artifacts
/public/
/resources/_gen/
.hugo_build.lock

# OS junk
Thumbs.db
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Claude Code workspace (settings, plans, transcripts, etc.)
.claude/

# Codex CLI config (contains API keys)
.codex/

# Local project notes (not for the public repo)
CLAUDE.md

# Secrets — defensive patterns; should never appear here, but make sure they can't
.env
.env.*
*.key
*.pem
*.pfx
id_rsa*
id_ed25519*
credentials.json
secrets.*
